apiVersion: v1
kind: Service
metadata:
  name: result-app-service
spec:
  type: NodePort
  selector:
    name: result-app-pod
    app: result-app-demo
  ports:
  - targetPort: 80
    port: 80
    nodePort: 30009
